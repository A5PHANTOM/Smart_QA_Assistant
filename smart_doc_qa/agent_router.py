def route_query(query: str) -> str:
    """
    Determines the intent of the user's query and routes it to the appropriate
    agent (e.g., Q&A over documents, summarization, general small talk).
    
    Args:
        query (str): The user's input query.
        
    Returns:
        str: The string identifier of the chosen execution route/agent.
    """
    pass
